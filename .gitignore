# macOS / editor
.DS_Store
*.swp
*~
.vscode/
.idea/

# Secrets / env (never commit)
.env
.env.*
*.pem
*-service-account*.json

# Babysitter runtime + local state (created at use, not part of the repo)
cc-status/
cc-queue/
cc-approved/
cc-autopilot/
cc-config.json
*.decision
.panel-alive
.debug.log
cc-gate.enabled
settings.local.json
*.bak
